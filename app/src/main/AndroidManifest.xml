<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required permissions for DJI SDK V5 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- Storage permissions (Android 13+ and legacy) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    
    <!-- Android 11+ (API 30+) Storage Access -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" tools:ignore="ScopedStorage" />

    <!-- CRITICAL: DJI SDK requires arm64-v8a architecture -->
    <!-- This prevents installation on x86/x86_64 emulators and tablets -->
    <uses-feature
        android:name="android.hardware.nfc"
        android:required="false" />
    <supports-screens
        android:smallScreens="true"
        android:normalScreens="true"
        android:largeScreens="true"
        android:xlargeScreens="true" />

    <!-- USB connection features -->
    <uses-feature android:name="android.hardware.usb.host"
        android:required="false" />
    <uses-feature android:name="android.hardware.usb.accessory"
        android:required="true" />

    <application
        android:name="io.empowerbits.sightflight.EagleEyeApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:theme="@style/Theme.EagleEyeAndroid"
        tools:targetApi="31">

        <!-- DJI API Key -->
        <meta-data
            android:name="com.dji.sdk.API_KEY"
            android:value="7a0f03fbfdb6bde4013f35da" />

        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAo1viD-Ut0TzXTyihevwuf-9tv_J3dPa0" />

        <activity
            android:name="io.empowerbits.sightflight.Activities.SplashActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity -->
        <activity
            android:name="io.empowerbits.sightflight.Activities.MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden">
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_ACCESSORY_ATTACHED"/>
            </intent-filter>
            <meta-data
                android:name="android.hardware.usb.action.USB_ACCESSORY_ATTACHED"
                android:resource="@xml/accessory_filter"/>
        </activity>

        <!-- Waypoint Activity -->
        <activity
            android:name="io.empowerbits.sightflight.Activities.WaypointActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:theme="@style/Theme.EagleEyeAndroid" />

        <!-- Waypoint Activity -->
        <activity
            android:name="io.empowerbits.sightflight.Activities.AddProject.ProjectInfoActivity"
            android:exported="false"
            android:theme="@style/Theme.EagleEyeAndroid" />

        <!-- Waypoint Activity -->
        <activity
            android:name="io.empowerbits.sightflight.Activities.LoginActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:theme="@style/Theme.EagleEyeAndroid" />

        <activity
            android:name="io.empowerbits.sightflight.Activities.AddProject.ProjectWaypointActivity"
            android:exported="false"
            android:theme="@style/Theme.EagleEyeAndroid" />

        <activity
            android:name="io.empowerbits.sightflight.Activities.FPVCameraActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:theme="@style/Theme.EagleEyeAndroid" />

        <activity
            android:name="io.empowerbits.sightflight.Activities.MediaManagerActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:theme="@style/Theme.EagleEyeAndroid" />

        <!-- Media Viewer Activity -->
        <activity
            android:name="io.empowerbits.sightflight.Activities.MediaViewerActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Services -->
        <service
            android:name="io.empowerbits.sightflight.Services.CommandService_V5SDK"
            android:enabled="true"
            android:exported="false" />

        <service
            android:name="io.empowerbits.sightflight.Services.TelemetryService"
            android:enabled="true"
            android:exported="false"
            tools:ignore="Instantiatable" />

        <service
            android:name="io.empowerbits.sightflight.Services.ButtonsListenerService"
            android:enabled="true"
            android:exported="false" />

    </application>

</manifest>
